'use strict';

describe('myApp.addDeal module', function() {

  beforeEach(module('myApp.addDeal'));

  describe('addDeal controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addDealCtrl = $controller('addDealCtrl');
      expect(addDealCtrl).toBeDefined();
    }));

  });
});