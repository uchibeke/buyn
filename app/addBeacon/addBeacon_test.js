'use strict';

describe('myApp.addBeacon module', function() {

  beforeEach(module('myApp.addBeacon'));

  describe('addBeacon controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addBeaconCtrl = $controller('addBeaconCtrl');
      expect(addBeaconCtrl).toBeDefined();
    }));

  });
});